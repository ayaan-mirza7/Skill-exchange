JAVASCRIPT:

1] PROMISE:IS A BUITIN CLASS CONTAINS
            ->RESPONSE 
	    ->REJECT 
	   AS ITS FUNCTIONS PARAMETER WHERE FUNCTION IS PARAMTER OF PROMISE CONSTRUCTOR
2] WE USE MODULE.EXPORT FOR SINGLE EXPORT AND 
	IF MULTIPLE EXPORT THAN WE USE EXPORT.
3] WE USE THEN AND CATCH AND TRY ONLY WHEN THERE IS A PROMOISE RETURN 
	ALSO WE CAN USE AWAIT INSTEAD OF THEN AND TRY AS AWAIT SIMPLY WAITS FOR THE OPERATION TO DONE AND THAN RETURNS A VALUE FINALLY NOT A 
	PROMISE .